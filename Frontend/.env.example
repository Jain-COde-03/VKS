# Example Vite env variables (do NOT commit real secrets)
# Copy to .env.local and fill values per developer/machine.

VITE_POSITIONSTACK_API_KEY=your_positionstack_key_here
# VITE_OTHER_PUBLIC_KEY=your_other_public_value

# Note: VITE_ prefixed variables are exposed to client-side code.
